# Task Schema (stub)

Purpose: canonical, versioned representation of an engineering task (see [[05-domain-model]]
`Task`, [[07-benchmark-strategy]]).

Concrete schema (fields, types, validation) is defined in **Phase 1 — Evaluation Contract**. This
stub is intentionally thin so Phase 1 design work is not pre-empted.
