# Experiment Schema (stub)

Purpose: canonical representation of an experiment — Task + repository state + agent + model +
context provider + tools + environment + configuration + evaluation version (see
[[05-domain-model]] `Experiment`/`Condition`, [[09-experiment-strategy]]).

Concrete schema is defined in **Phase 1 — Evaluation Contract**. This stub is intentionally thin
so Phase 1 design work is not pre-empted.
