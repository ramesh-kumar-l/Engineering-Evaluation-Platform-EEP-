# Trace Schema (stub)

Purpose: canonical representation of the full engineering interaction lifecycle captured per run
— context, agent reasoning/actions, tool calls, files read/changed, commands, tests, retries,
verification, final outcome — with configurable redaction (see [[05-domain-model]] `Trace`,
[[11-security]]).

Concrete schema is defined in **Phase 1 — Evaluation Contract**. This stub is intentionally thin
so Phase 1 design work is not pre-empted.
